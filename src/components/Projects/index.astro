---
import AppCard from "./AppCard.astro";

import projects from "./list";
---

<section class="flex justify-center py-12 px-6 bg-gradient-to-br from-sky-500 to-blue-600">
	<div class="max-w-5xl w-full">
		<h1 class="text-white text-3xl md:text-4xl lg:text-5xl font-semibold mb-2 sm:mb-3 lg:mb-4">Things I've Built</h1>
		<p class="text-slate-200 text-base md:text-lg leading-7 max-w-xl mb-8 pl-1">
			Over the years, I have created many useful and creative apps for different use cases.
		</p>
		<div class="grid grid-cols-1 md:grid-cols-2 gap-4">
			{projects.map((project) => <AppCard {...project} />)}
		</div>
	</div>
</section>
